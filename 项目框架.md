# 项目框架说明 - ReAct + Tools 版本

## 项目概述
这是一个多轮对话的AI系统，采用ReAct推理模式，支持Function Calling工具调用，会话自动AI摘要。

## 架构特点

### 1. ReAct推理模式
- **Thought**: 显示AI的思考过程
- **Action**: 执行工具调用
- **Observation**: 展示工具执行结果
- **Final Answer**: 最终回答

### 2. Function Calling工具系统
6个标准工具，支持参数验证和Schema定义：
- 文件读写操作
- 命令行执行
- 文件搜索

### 3. AI会话摘要
- 自动提取核心观点
- 生成关键词主题
- 会话列表展示摘要

## 目录结构

```
project/
├── config/                    # 配置目录
│   ├── api_config.json       # API配置（用户可自定义）
│   └── settings.json         # 系统设置
├── sessions/                  # 对话历史存储
│   └── YYYY-MM-DD_HH-MM-SS.md # 含AI摘要的Markdown记录
├── logs/                      # 日志目录
│   └── app.log               # 应用运行日志
├── tools/                     # 工具目录 ⭐新增
│   ├── __init__.py           # 工具注册
│   ├── base.py               # 工具基类和注册表
│   ├── file_tools.py         # 文件操作工具
│   └── command_tools.py      # 命令执行工具
├── src/                       # 源代码
│   ├── __init__.py
│   ├── main.py               # 主入口（集成ReAct）
│   ├── config_manager.py     # 配置管理
│   ├── session_manager.py    # 会话管理（支持AI摘要）
│   ├── ai_client.py          # AI客户端（支持Function Calling）
│   ├── react_agent.py        # ReAct推理系统 ⭐新增
│   └── commands.py           # 命令处理
├── 项目要求.md               # 项目需求文档
├── 项目框架.md               # 本文件
├── 对话记录.md               # 对话历史记录
└── README.md                 # 项目说明
```

## 核心模块说明

### 1. config_manager.py
- 管理API配置（JSON格式存储）
- 支持多API提供商切换
- 读取和保存系统设置

### 2. session_manager.py ⭐增强
- **AI自动摘要**: 每5条消息生成一次摘要
- **核心主题提取**: 自动提取关键词
- **增强列表显示**: 显示摘要、主题、消息数
- Markdown格式保存会话历史

### 3. ai_client.py ⭐增强
- 封装AI API调用
- **支持Function Calling**: 工具调用格式
- **summarize_messages**: 生成对话摘要
- 流式响应处理

### 4. react_agent.py ⭐新增
- **ReAct推理循环**: Thought → Action → Observation
- 最多10轮迭代
- 工具调用解析和执行
- 思考过程展示

### 5. Tools目录 ⭐新增

#### base.py
- `BaseTool`: 工具基类，定义标准接口
- `ToolRegistry`: 工具注册表，管理所有工具
- 支持参数验证和JSON Schema

#### file_tools.py
- `WriteFileTool`: 写入文件
- `ReadFileTool`: 读取文件（支持分页）
- `DeleteFileTool`: 删除文件（安全确认）
- `ListFilesTool`: 列出目录

#### command_tools.py
- `ExecuteCommandTool`: 执行命令（带安全黑名单）
- `SearchFilesTool`: 搜索文件内容（支持正则）

### 6. commands.py
- 处理用户命令
- `/sessions` - 列出所有会话（含AI摘要）
- `/load <session_id>` - 加载指定会话
- `/new` - 创建新会话
- `/config` - 查看/修改配置
- `/quit` - 退出程序

### 7. main.py ⭐更新
- 初始化所有组件
- 集成ReAct Agent
- 集成Tool Registry
- 主循环处理用户输入

## 功能特性

### 1. API配置管理
```json
{
  "default": "dashscope",
  "providers": {
    "dashscope": {
      "base_url": "https://coding.dashscope.aliyuncs.com/v1",
      "api_key": "sk-xxxxx"
    }
  }
}
```

### 2. 会话AI摘要
会话文件格式：
```markdown
# 对话会话 2026-03-22_12-00-00

**创建时间**: 2026-03-22T12:00:00
**更新时间**: 2026-03-22T12:30:00
**消息数量**: 10

## 📋 会话摘要
讨论Python项目重构方案，包括ReAct模式和工具系统设计

### 🔑 核心主题
- ReAct推理模式
- Function Calling
- 工具系统设计
- 会话管理

---

## 💬 对话记录
...
```

### 3. ReAct交互流程
```
用户: 帮我读取config/settings.json

💭 Thought: 用户想要读取配置文件
🔧 Action: read_file(file_path="config/settings.json")
👁 Observation: [文件内容]
✅ Final Answer: 配置文件包含...
```

### 4. 工具调用
所有工具符合Function Calling标准：
```json
{
  "type": "function",
  "function": {
    "name": "read_file",
    "description": "读取文件内容",
    "parameters": {
      "type": "object",
      "properties": {
        "file_path": {"type": "string"}
      },
      "required": ["file_path"]
    }
  }
}
```

## 运行方式

```bash
# 安装依赖
pip install requests

# 运行程序
python src/main.py
```

## 使用命令

```
/sessions          # 列出历史会话（显示AI摘要）
/load <ID>         # 加载指定会话
/new               # 创建新会话
/config            # 配置管理
/help              # 帮助
/quit              # 退出
```

## 工具列表

| 工具名 | 功能 | 参数 |
|--------|------|------|
| write_file | 写入文件 | file_path, content, encoding |
| read_file | 读取文件 | file_path, limit, offset |
| delete_file | 删除文件 | file_path, recursive, confirm |
| list_files | 列出文件 | directory, pattern, recursive |
| execute_command | 执行命令 | command, working_dir, timeout |
| search_files | 搜索文件 | pattern, directory, file_pattern |

## 安全特性

1. **命令黑名单**: 阻止rm -rf /等危险命令
2. **删除确认**: delete_file需要confirm=true
3. **路径验证**: 工具执行前验证路径
4. **超时保护**: 命令执行默认60秒超时
