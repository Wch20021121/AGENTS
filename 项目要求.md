# MiniAgent 产品需求文档

## 1. 项目定位

### 1.1 项目名称
- 名称：MiniAgent
- 类型：本地文件操作智能体
- 形态：前后端分离的轻量级 Agent 系统

### 1.2 项目目标
MiniAgent 是一个面向本地工作目录的极简 Agent。整体交互形态参考 Manus / OpenManus 一类通用 Agent：以自然语言对话为入口，基于大模型的函数调用能力完成工具选择、参数组织、执行反馈和结果总结。

本项目首期不追求“全能 Agent”，而是聚焦一个可用、可控、可扩展的最小闭环：
- 用户通过前端界面输入自然语言或命令
- 后端调用外接大模型进行意图理解
- 模型按标准 Function Call 选择基础 tools
- 系统执行本地文件或命令操作
- 前端展示回复、工具执行过程和结果日志

### 1.3 首期建设原则
- 先做小而稳：首期只支持少量基础 tools，保证稳定性和安全性
- 先跑通闭环：优先完成“对话 -> 调用工具 -> 返回结果 -> 前端展示”完整链路
- 前后端分离：界面、调度、工具执行、配置管理职责明确
- 模块化扩展：后续新增 tools、命令、前端面板时尽量不改核心主流程
- 可观察性优先：工具调用、错误、权限拦截、执行日志必须可见

### 1.4 非目标
以下内容不属于首期范围：
- 多智能体协作
- 浏览器自动化
- 长时任务编排
- 云端文件系统接入
- 插件市场
- 复杂权限系统（如多用户、RBAC）
- 完整 IDE 级代码代理能力

## 2. 产品范围

### 2.1 核心能力
- 对话式交互
- 命令式交互
- 大模型函数调用
- 基础文件工具调用
- 安全命令执行
- 工具执行日志展示
- 会话记录保存
- 模型配置管理

### 2.2 首期交付范围
首期版本聚焦以下能力：
- 前端聊天界面
- 前端工具/命令展示面板
- 前端设置面板
- 后端 Agent 调度服务
- 后端 Tool Registry
- 5 个基础 tools
- 基础命令系统
- 本地配置文件读写
- 会话与日志存储

### 2.3 后续扩展方向
后续版本可逐步加入：
- 新文件工具：复制、移动、重命名、批量替换
- 代码工具：代码搜索、目录分析、差异比较
- 浏览器工具
- HTTP 请求工具
- 工作流编排
- 多轮计划执行
- Tool 权限白名单细化
- WebSocket 流式输出

## 3. 用户与使用场景

### 3.1 目标用户
- 个人开发者
- 轻量自动化需求使用者
- 希望通过自然语言完成本地文件操作的用户

### 3.2 核心场景
1. 让 Agent 创建或覆盖一个文件
2. 让 Agent 读取指定文件内容
3. 让 Agent 检索当前工作目录中的文件
4. 让 Agent 删除指定文件
5. 让 Agent 执行安全命令并返回输出
6. 通过命令查看当前工具列表、配置和运行状态

### 3.3 典型用户故事
- 作为用户，我希望输入“帮我创建一个 test.txt，内容是 hello agent”，系统可以自动调用写文件工具并返回结果。
- 作为用户，我希望输入“读取 config/settings.json”，系统能够读取并展示内容摘要或全文。
- 作为用户，我希望输入“帮我搜索包含 api 的文件”，系统能够返回匹配文件列表。
- 作为用户，我希望看到 Agent 具体调用了哪个 tool、传了什么参数、执行是否成功。
- 作为用户，我希望在设置面板中配置模型地址、模型名、API Key 以及是否允许执行命令。

## 4. 总体架构

### 4.1 架构概述
系统采用前后端分离架构，分为 5 个核心层次：

1. 前端展示层
2. 接口与会话层
3. Agent 调度层
4. Tool 执行层
5. 配置与存储层

### 4.2 架构目标
- 让前端只负责交互与展示，不直接执行危险操作
- 让后端统一负责模型调用、工具调度、权限判断和结果整理
- 让 tools 作为独立模块注册，不耦合主流程
- 让配置、会话、日志都有明确存储位置

### 4.3 逻辑流程
用户输入 -> 前端提交请求 -> 后端会话接收 -> Agent 调用模型 -> 模型选择 tool -> 后端校验权限 -> 执行 tool -> 返回结构化结果 -> 模型生成最终回答 -> 前端展示

## 5. 前端需求

### 5.1 前端目标
前端负责提供一个轻量、清晰、低学习成本的 Agent 操作界面，让用户可以同时看到：
- 对话内容
- 当前可用命令
- 当前可用 tools
- 工具执行结果
- 模型与权限配置

### 5.2 页面模块

#### 5.2.1 聊天主区域
功能要求：
- 显示用户消息与 Agent 回复
- 支持多轮对话滚动展示
- 支持展示工具调用过程
- 支持展示命令执行结果
- 支持展示错误信息和权限拦截信息

展示要求：
- 用户消息和 Agent 消息视觉区分明确
- 工具调用块独立高亮展示
- 错误信息采用醒目的状态样式
- 长文本支持折叠/展开

#### 5.2.2 输入区域
功能要求：
- 支持自然语言输入
- 支持 `/` 命令输入
- 支持回车发送
- 支持发送中状态展示

可选增强：
- 输入 `/` 时弹出命令建议
- 输入过程中提示当前可用 tools 或示例语句

#### 5.2.3 命令面板
功能要求：
- 展示内置命令列表
- 支持点击命令自动填充输入框
- 展示命令说明

#### 5.2.4 Tools 面板
功能要求：
- 列出当前注册的 tools
- 展示每个 tool 的名称、说明、参数要求、权限类型
- 支持点击查看 tool 详情

说明：
- 首期只要求展示和说明，不要求前端直接绕过模型触发 tool
- 是否支持“点击 tool 直接执行”可作为后续能力

#### 5.2.5 设置面板
功能要求：
- 配置模型 API Base URL
- 配置 API Key
- 配置模型名称
- 配置是否允许命令执行
- 配置工作目录范围
- 配置是否展示调试日志

#### 5.2.6 会话侧栏
功能要求：
- 展示历史会话列表
- 支持切换会话
- 支持新建会话
- 支持清空当前会话记录

### 5.3 前端样式要求
- 风格简洁，避免复杂外部依赖
- 支持 PC 和移动端响应式布局
- 工具执行块与普通聊天消息分层明显
- 日志输出要保留等宽字体显示区域
- 支持空状态、加载态、错误态

## 6. 后端需求

### 6.1 后端目标
后端是系统核心，负责：
- 接收前端消息
- 维护会话上下文
- 调用外部大模型
- 管理 tool 注册与 schema
- 执行 tool 并处理异常
- 做权限控制和安全校验
- 返回统一结构给前端

### 6.2 后端模块划分

#### 6.2.1 API 接口层
职责：
- 提供前端调用接口
- 接收聊天消息、命令请求、配置请求
- 返回统一 JSON 响应

建议接口类别：
- 会话接口
- 聊天接口
- 命令接口
- 配置接口
- tools 查询接口
- 状态接口

#### 6.2.2 Agent Core 调度层
职责：
- 组装 prompt / messages
- 调用模型
- 解析 function call
- 调度 tool 执行
- 汇总执行结果
- 生成最终答复

设计要求：
- 支持单轮多次 tool 调用
- 支持最大迭代次数限制
- 支持异常中断与友好提示
- 支持输出中间步骤给前端展示

#### 6.2.3 Tool Registry
职责：
- 注册所有可用 tools
- 输出标准函数清单 JSON Schema
- 根据名称查找并执行具体 tool
- 管理 tool 元数据

tool 元数据至少包含：
- 名称
- 描述
- 参数 Schema
- 权限类型
- 是否默认启用

#### 6.2.4 Config 模块
职责：
- 管理模型配置
- 管理安全开关
- 管理工作目录限制
- 管理系统默认设置

#### 6.2.5 Session 模块
职责：
- 保存会话消息
- 保存工具调用记录
- 保存时间戳和状态
- 支持会话读取与切换

#### 6.2.6 Logging 模块
职责：
- 记录运行日志
- 记录工具调用日志
- 记录错误日志
- 记录权限拦截日志

## 7. Tool 系统设计

### 7.1 设计原则
- Tool 必须模块化
- Tool 必须具备独立描述和参数 Schema
- Tool 必须返回统一结构化结果
- Tool 必须在执行前经过权限校验
- Tool 执行异常必须可捕获、可回传

### 7.2 首期基础 Tools
首期只支持以下 5 个基础 tools：

1. `write_file`
2. `read_file`
3. `search_files`
4. `run_command`
5. `delete_file`

这是首期最小可用工具集，足以跑通“文件创建、读取、检索、删除、命令执行”五类核心场景。后续版本再继续扩展，不在首期需求中预埋过多复杂能力。

### 7.3 Tool 详细定义

#### 7.3.1 写文件
- 函数名：`write_file(file_path, content)`
- 功能：创建或覆盖写入工作目录内的文件
- 权限：文件写入
- 输入：文件路径、文件内容
- 输出：是否成功、写入路径、写入字节数、提示信息

约束：
- 仅允许写入工作目录内的路径
- 默认覆盖写入，后续可扩展追加模式

#### 7.3.2 读文件
- 函数名：`read_file(file_path)`
- 功能：读取工作目录内指定文件内容
- 权限：文件读取
- 输入：文件路径
- 输出：是否成功、文件内容、文件大小、提示信息

约束：
- 仅允许读取工作目录内文件
- 对超大文件可进行截断返回

#### 7.3.3 检索文件
- 函数名：`search_files(keyword=None)`
- 功能：列出工作目录文件，或按关键词检索文件名/内容
- 权限：文件读取
- 输入：关键词，可选
- 输出：匹配文件列表、命中数量、提示信息

约束：
- 首期优先支持文件名检索
- 文件内容检索可作为增强实现，但不是强制项

#### 7.3.4 运行命令
- 函数名：`run_command(command)`
- 功能：执行允许范围内的命令行指令
- 权限：命令执行
- 输入：命令字符串
- 输出：标准输出、标准错误、退出码、提示信息

约束：
- 必须受配置开关控制
- 必须限制在工作目录中执行
- 必须有危险命令黑名单或白名单策略
- 必须设置超时时间

#### 7.3.5 删除文件
- 函数名：`delete_file(file_path)`
- 功能：删除工作目录内指定文件
- 权限：文件删除
- 输入：文件路径
- 输出：是否成功、删除路径、提示信息

约束：
- 仅允许删除工作目录内文件
- 首期只支持文件删除，不要求目录递归删除

### 7.4 Tool 标准返回结构
所有 tools 统一返回类似结构：

```json
{
  "success": true,
  "tool": "read_file",
  "data": {},
  "error": null,
  "message": "操作成功"
}
```

失败时：

```json
{
  "success": false,
  "tool": "run_command",
  "data": null,
  "error": "command not allowed",
  "message": "命令执行被安全策略拦截"
}
```

### 7.5 Tool 注册格式
函数清单采用标准 JSON Schema，供模型 Function Call 使用：

```json
[
  {
    "name": "write_file",
    "description": "写入文件到当前工作目录",
    "parameters": {
      "type": "object",
      "properties": {
        "file_path": {"type": "string", "description": "相对工作目录的文件路径"},
        "content": {"type": "string", "description": "文件内容"}
      },
      "required": ["file_path", "content"]
    }
  },
  {
    "name": "read_file",
    "description": "读取当前工作目录下的文件内容",
    "parameters": {
      "type": "object",
      "properties": {
        "file_path": {"type": "string", "description": "相对工作目录的文件路径"}
      },
      "required": ["file_path"]
    }
  },
  {
    "name": "search_files",
    "description": "检索或列出当前工作目录中的文件",
    "parameters": {
      "type": "object",
      "properties": {
        "keyword": {"type": "string", "description": "检索关键词，可选"}
      },
      "required": []
    }
  },
  {
    "name": "run_command",
    "description": "执行受限制的安全命令行指令",
    "parameters": {
      "type": "object",
      "properties": {
        "command": {"type": "string", "description": "命令字符串"}
      },
      "required": ["command"]
    }
  },
  {
    "name": "delete_file",
    "description": "删除当前工作目录下指定文件",
    "parameters": {
      "type": "object",
      "properties": {
        "file_path": {"type": "string", "description": "相对工作目录的文件路径"}
      },
      "required": ["file_path"]
    }
  }
]
```

## 8. 命令系统设计

### 8.1 命令目标
命令系统用于处理无需模型理解即可直接执行的本地控制动作，提高可控性和效率。

### 8.2 首期内置命令
1. `/help`：显示帮助信息
2. `/skills`：列出当前注册 tools
3. `/clear`：清空当前聊天记录
4. `/config`：打开或返回当前配置
5. `/status`：查看当前运行状态、模型状态、权限状态

### 8.3 命令规则
- 统一以 `/` 开头
- 不区分大小写
- 命令解析独立于 Agent 调度逻辑
- 命令执行结果以结构化格式返回给前端
- 命令优先级高于自然语言意图识别

### 8.4 命令返回结构
```json
{
  "type": "command_result",
  "command": "/status",
  "success": true,
  "data": {},
  "message": "当前系统运行正常"
}
```

## 9. Function Call 引擎

### 9.1 核心职责
- 将用户自然语言请求转换为模型可理解上下文
- 向模型提供 tools 列表与参数 Schema
- 解析模型返回的 function call
- 校验工具权限与参数
- 执行工具并把结果回传模型
- 获取最终自然语言回答

### 9.2 处理流程
1. 接收用户输入
2. 判断是否为命令输入
3. 若不是命令，则进入模型推理
4. 模型根据上下文选择是否调用 tool
5. 系统执行权限校验
6. 执行对应 tool
7. 将 tool 结果回传模型
8. 生成最终回答
9. 返回给前端展示

### 9.3 行为要求
- 支持“无 tool 直接回答”
- 支持“单轮一次或多次 tool 调用”
- 支持执行失败后的错误总结
- 支持限制最大调用轮次，避免死循环

## 10. 数据与存储

### 10.1 配置存储
配置文件用于保存：
- 模型服务地址
- API Key
- 默认模型名
- 命令执行开关
- 工作目录
- 调试开关

### 10.2 会话存储
会话数据至少包含：
- 会话 ID
- 创建时间
- 更新时间
- 消息列表
- 工具调用记录
- 最终回复

### 10.3 日志存储
日志至少包括：
- 系统启动日志
- 模型调用日志
- tool 执行日志
- 安全拦截日志
- 错误日志

## 11. 安全与限制

### 11.1 路径安全
- 所有文件类 tools 都必须限制在指定工作目录内
- 禁止通过相对路径穿透到工作目录外
- 禁止访问敏感系统目录

### 11.2 命令安全
- `run_command` 默认受配置开关控制
- 对危险命令进行黑名单拦截
- 命令执行必须有超时
- 命令执行目录必须固定在工作目录

### 11.3 错误处理
- tool 执行失败不得导致主程序崩溃
- 所有错误都应返回结构化信息
- 错误信息需要前端可见且可区分来源

## 12. 前后端接口建议

### 12.1 会话接口
- `GET /sessions`：获取会话列表
- `POST /sessions`：创建新会话
- `GET /sessions/{id}`：获取会话详情

### 12.2 聊天接口
- `POST /chat`：发送用户消息，获取 Agent 回复和 tool 调用结果

### 12.3 配置接口
- `GET /config`：读取当前配置
- `POST /config`：更新配置

### 12.4 tools 接口
- `GET /tools`：获取当前注册 tools 列表

### 12.5 状态接口
- `GET /status`：获取系统状态、模型状态、权限状态

说明：
- 接口名称可按实际框架调整
- 首期只要求满足前端调用闭环，不强制追求完整 REST 规范

## 13. 推荐目录规划

### 13.1 前端
```text
frontend/
  components/
  pages/
  services/
  stores/
  styles/
```

### 13.2 后端
```text
backend/
  api/
  core/
  tools/
  services/
  storage/
  config/
```

### 13.3 当前项目映射建议
若沿用当前 Python 项目结构，可按以下职责理解：
- `src/main.py`：程序入口
- `src/ai_client.py`：模型客户端
- `src/react_agent.py`：Agent 调度核心
- `src/commands.py`：命令系统
- `src/config_manager.py`：配置管理
- `src/session_manager.py`：会话管理
- `tools/`：基础 tools 注册与实现

## 14. 首期验收标准

### 14.1 功能验收
- 用户可以在前端发送自然语言消息
- 系统可根据消息调用正确的基础 tool
- 前端可看到工具调用过程与执行结果
- 用户可以在前端完成配置修改
- 用户可以查看命令列表和 tools 列表
- 用户可以创建、切换、清空会话

### 14.2 Tool 验收
- `write_file` 可创建或覆盖文件
- `read_file` 可读取文件内容
- `search_files` 可列出或检索文件
- `run_command` 可在安全限制下执行命令
- `delete_file` 可删除指定文件

### 14.3 安全验收
- 越界路径访问被拦截
- 危险命令被拦截
- 工具执行失败时系统仍可继续工作
- 日志中可看到关键执行记录

## 15. 版本规划

### 15.1 V1.0
目标：跑通前后端闭环和 5 个基础 tools

包含：
- 前端聊天界面
- 前端命令面板
- 前端 tools 面板
- 前端设置面板
- 后端 Agent Core
- 后端 Tool Registry
- 基础会话存储
- 基础日志系统

### 15.2 V1.1
目标：增强可用性和可观察性

可加入：
- 流式输出
- 更丰富的 tool 日志展示
- 更细粒度配置项
- 搜索能力增强

### 15.3 V1.2+
目标：逐步向通用 Agent 演进

可加入：
- 新工具扩展
- 工作流能力
- 更复杂的上下文管理
- 多步骤计划执行

## 16. 使用示例

### 16.1 示例一：写文件
用户输入：
帮我写一个 `test.txt` 文件，内容是 `hello agent`

系统行为：
1. 模型识别用户意图为写文件
2. 选择 `write_file`
3. 参数为 `file_path=test.txt`、`content=hello agent`
4. 后端校验路径合法
5. 执行写入
6. 返回工具结果
7. 模型生成最终回答
8. 前端展示回复和执行日志

### 16.2 示例二：读文件
用户输入：
请读取 `config/settings.json`

系统行为：
1. 模型识别用户意图为读文件
2. 选择 `read_file`
3. 后端执行读取
4. 返回文件内容
5. 前端展示内容和工具调用记录

### 16.3 示例三：执行命令
用户输入：
帮我执行 `dir`

系统行为：
1. 模型识别需要执行命令
2. 系统检查命令执行权限是否开启
3. 检查命令是否在允许范围内
4. 执行并返回输出
5. 前端展示标准输出或错误信息

## 17. 结论
MiniAgent 的首期目标不是复制一个完整的通用 Agent 平台，而是构建一个参考 Manus / OpenManus 交互方式、但能力边界明确的轻量版本：
- 有前端
- 有后端
- 有对话入口
- 有函数调用
- 有基础 tools
- 有日志和安全约束
- 可继续扩展

首期只把基础工具闭环做好，后续再逐步增加新 tools 和更强的 Agent 能力。这份需求文档应作为 V1.0 的产品与开发共同基线。